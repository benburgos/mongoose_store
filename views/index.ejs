<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head.ejs') %> 
</head>
<body>
<div class="container">
    
    <section class="hero is-small is-info">
        <div class="hero-body">
          <p class="title">
            All Products
          </p>
          <p class="subtitle">
            Mongoose Store
          </p>
        </div>
        <div class="navbar-end">
            <div class="navbar-item">
              <div class="buttons">
                <a class="button is-light" href="/store/new">
                  Add New Item
                </a>
              </div>
            </div>
          </div>
      </section>

      <br>

    <div class="columns is-multiline">
    <% for( let index = 0; index < products.length; index++ ) { %>
        <div class="column has-text-centered is-one-quarter">
            <ul>
                <li>
                    <a href="/store/<%= products[index]._id %>">
                        <img src="<%= products[index].img %>" alt="<%= products[index].name %>">
                    </a>
                </li>
                <li><%= products[index].name %></li>
                <li>$<%= products[index].price %></li>
            </ul>
        </div>
    <% } %>
    </div>
</div>
</body>
</html>